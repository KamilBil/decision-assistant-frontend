import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#808080', // Orange for the main elements
        },
        secondary: {
            main: '#4caf50', // Green for secondary elements
        },
    },
});

export default theme;
